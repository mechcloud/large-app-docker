## Prerequisites
* docker-compose v1.25.0+

## Steps
* Clone this repository -
```
cd <workspace>
git clone git@github.com:koyadume/large-app-docker.git
```
* Run containers -
```
cd large-app-docker/docker-compose
docker-compose down && docker-compose -p large-app-docker up -d
```

