# large-app-docker

## Clone repo, build docker images and run apps
```
cd <workspace>
git clone git@github.com:koyadume/large-app-docker.git

cd large-app-docker
chmod +x deploy.sh

./deploy.sh
```

